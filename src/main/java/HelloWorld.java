/* * COPYRIGHT 1995-2018 ESRI * * TRADE SECRETS: ESRI PROPRIETARY AND CONFIDENTIAL * Unpublished material - all rights reserved under the * Copyright Laws of the United States. * * For additional information, contact: * Environmental Systems Research Institute, Inc. * Attn: Contracts Dept * 380 New York Street * Redlands, California, USA 92373 * * email: contracts@esri.com */import javafx.application.Application;import javafx.scene.Scene;import javafx.scene.control.Button;import javafx.scene.layout.BorderPane;import javafx.scene.layout.HBox;import javafx.stage.Stage;import com.esri.arcgisruntime.mapping.ArcGISMap;import com.esri.arcgisruntime.mapping.Basemap;import com.esri.arcgisruntime.mapping.view.MapView;public class HelloWorld extends Application {  //  private SceneView view;  private MapView view;  /**   * Opens and runs application.   *   * @param args arguments passed to this application   */  public static void main(String[] args) {    //    ArcGISRuntimeEnvironment.setInstallDirectory("/Users/john0005/ArcGIS SDKs/JavaSDK100.4.0/arcgis-runtime-sdk-java-100.4.0");    //    ArcGISRuntimeEnvironment.setInstallDirectory("/Users/john0005/ArcGIS SDKs/JavaSDK100.0.0/arcgis-java-sdk-100.0.0");    Application.launch(args);  }  @Override  public void start(Stage stage) {    // create stack pane and application scene    BorderPane stackPane = new BorderPane();    Scene fxScene = new Scene(stackPane);    // set title, size, and add scene to stage    stage.setTitle("");    stage.setWidth(800);    stage.setHeight(700);    stage.setScene(fxScene);    stage.show();    try {      //            ArcGISScene geo = new ArcGISScene(ArcGISScene.SceneViewTilingScheme.WEB_MERCATOR);      //      ArcGISScene geo = new ArcGISScene();      //      view = new SceneView();      //      view.setArcGISScene(geo);      ArcGISMap geo = new ArcGISMap(Basemap.createOpenStreetMap());      view = new MapView();      view.setMap(geo);      // test with max tolerance and zero tolerance (no max results)      Button but1 = new Button("Action");      but1.setOnAction(e -> {      });      Button but2 = new Button("Action");      but2.setOnAction(e -> {      });      HBox buttons = new HBox(but1, but2);      HBox box = new HBox(buttons);      stackPane.setCenter(view);      stackPane.setBottom(box);    } catch (Exception e) {      System.out.println("Error: " + e.getLocalizedMessage());      e.printStackTrace();    }  }  /**   * Stops and releases all resources used in application.   *   * @throws Exception if security manager doesn't allow JVM to exit with current status   */  @Override  public void stop() {    if (view != null) {      view.dispose();    }  }}